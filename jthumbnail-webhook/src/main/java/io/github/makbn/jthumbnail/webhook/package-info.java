/**
 * CMS / webhook connector: generic webhook ingestion with HMAC validation,
 * replay protection, and pluggable adapters (WordPress, generic JSON).
 */
package io.github.makbn.jthumbnail.webhook;
