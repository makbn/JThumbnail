version: "3.9"

services:
  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.1
    container_name: jthumbnail-zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: jthumbnail-kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

  jthumbnail-app:
    environment:
      SPRING_KAFKA_BOOTSTRAPSERVERS: kafka:9092
      JTHUMBNAILER_JOBS_TOPIC: thumbnail-jobs
      JTHUMBNAILER_JOBS_DEADLETTERTOPIC: thumbnail-jobs-dlq
      JTHUMBNAILER_JOBS_MAXRETRIES: 3
      JTHUMBNAILER_JOBS_CONSUMERCONCURRENCY: 1
      # Enable Kafka-based job queue
      JTHUMBNAILER_GRPC_ENABLED: "false"

