/**
 * S3-compatible storage event connector: trigger thumbnail generation on object-created
 * events (webhook or SQS), then store thumbnails back to S3/MinIO.
 */
package io.github.makbn.jthumbnail.storage;
