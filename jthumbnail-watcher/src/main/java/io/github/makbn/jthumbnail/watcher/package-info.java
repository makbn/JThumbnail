/**
 * Local filesystem watcher connector: auto-generate thumbnails when files are
 * added to configured directories, with debounce and file size stabilization.
 */
package io.github.makbn.jthumbnail.watcher;
